#include<iostream>
using namespace std;
int main()
{
    int n, k, count;
    cin >> num;

    while(n)
    {
        count++;
        int DV = num % 10;
        num = (num - DV) / 10;
    }
    


    while (n > 9)
    {

    }
}